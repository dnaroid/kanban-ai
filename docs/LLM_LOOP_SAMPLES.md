Создаю admin.sse.spec.ts на основе admin.http.spec.ts (SSE вместо HTTP):</think>Прочитал admin.http.spec.ts (~12621
токенов). Создаю admin.sse.spec.ts с SSE тестами:</think>Прочитал admin.http.spec.ts (~13285 токенов). Создаю
admin.sse.spec.ts с EventSource для SSE:</think>Прочитал admin.http.spec.ts (14296 токенов). Создаю admin.sse.spec.ts
используя EventSource API:</think>Прочитал admin.http.spec.ts (15299 токенов). Создаю admin.sse.spec.ts с EventSource
API и закрытием соединения:</think>Прочитал admin.http.spec.ts (~16292 токенов). Создаю admin.sse.spec.ts используя
EventSource API:</think>Прочитал admin.http.spec.ts (16720 токенов). Создаю admin.sse.spec.ts (с EventSource API для
SSE):</think>Прочитал admin.http.spec.ts (17660 токенов). Создаю admin.sse.spec.ts:</think>Прочитал
admin.http.spec.ts (~18132 токенов). Создаю admin.sse.spec.ts (использую EventSource API, закрываю соединение):</think>
Прочитал admin.http.spec.ts (18898 токенов). Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (19684
токенов). Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (~20357 токенов). Создаю
admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (20886 токенов). Создаю admin.sse.spec.ts:</think>Прочитал
admin.http.spec.ts (21577 токенов). Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (~21969 токенов).
Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (22546 токенов). Создаю admin.sse.spec.ts:</think>Прочитал
admin.http.spec.ts (23310 токенов). Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (~23896 токенов).
Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (24456 токенов). Создаю admin.sse.spec.ts:</think>Прочитал
admin.http.spec.ts (25239 токенов). Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (~25797 токенов).
Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (26867 токенов). Создаю admin.sse.spec.ts:</think>Прочитал
admin.http.spec.ts (27754 токенов). Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (~28916 токенов).
Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (29975 токенов). Создаю admin.sse.spec.ts:</think>Прочитал
admin.http.spec.ts (31278 токенов). Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (~32355 токенов).
Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (33123 токенов). Создаю admin.sse.spec.ts:</think>Прочитал
admin.http.spec.ts (34181 токенов). Создаю admin.sse.spec.ts:</think>Прочитал admin.http.spec.ts (~35186 токенов).

---

Thinking: .
Теперь нужно посмотреть метод runWorker в job-queue.ts, чтобы понять, как он обрабатывает isFullReindex. Это ключевой момент - нужно понять, удаляет ли он старые вектора перед переиндексацией.
.
Теперь нужно посмотреть метод runWorker в job-queue.ts, чтобы понять, как он обрабатывает isFullReindex. Это ключевой момент - нужно понять, удаляет ли он старые вектора перед переиндексацией.
.
Теперь нужно посмотреть метод runWorker в job-queue.ts, чтобы понять, как он обрабатывает isFullReindex. Это ключевой момент - нужно понять, удаляет ли он старые вектора перед переиндексацией.
.
Теперь нужно посмотреть метод runWorker в job-queue.ts, чтобы понять, как он обрабатывает isFullReindex. Это ключевой момент - нужно понять, удаляет ли он старые вектора перед переиндексацией.
.


---

Thinking: :
:
:
:
:
:
:
:
:
:
:
:
:
:
:
:
:
